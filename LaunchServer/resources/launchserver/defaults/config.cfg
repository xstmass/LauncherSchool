address: "x";
bindAddress: "0.0.0.0";
port: 7240;

# Auth rate limit
authRateLimit: 2;
authRateLimitMilis: 5000;
authRejectString: "Вы превысили лимит авторизаций, подождите некоторое время!";

# Auth handler
authHandler: "textFile";
authHandlerConfig: {
    file: "authHandler.cfg";
    offlineUUIDs: true;
};

# Auth provider
authProvider: "reject";
authProviderConfig: {
    message: "You need to change auth provider in LaunchServer.cfg";
};

# Texture provider
textureProvider: "mojang";
textureProviderConfig: {
    # No config needed for mojang texture provider
};

# Launch4J EXE binary building
launch4J: false;

# Compress files when updating using Inflate algorithm
compress: true;
